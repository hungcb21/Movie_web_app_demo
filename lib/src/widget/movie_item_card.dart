import 'package:flutter/material.dart';
class MovieItemCard extends StatelessWidget {
  const MovieItemCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
